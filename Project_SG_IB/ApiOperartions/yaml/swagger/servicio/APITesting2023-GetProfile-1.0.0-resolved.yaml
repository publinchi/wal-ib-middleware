---
openapi: 3.0.0
info:
  title: Get Profile
  description: Service to obtain the client's profile.
  version: 1.0.0
servers:
- url: https://virtserver.swaggerhub.com/APITesting2023/GetProfile/1.0.0
  description: SwaggerHub API Auto Mocking
tags:
- name: Get Profile
  description: Service to obtain the client's profile
paths:
  /apiOperations/enrollment/getProfile:
    post:
      tags:
      - Get Profile
      summary: Service to obtain the client's profile
      operationId: getProfile
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RequestProfile'
        required: true
      responses:
        "400":
          description: The data sent is incorrect or corrupted
        "500":
          description: An error occurred on the server
        "401":
          description: Unauthorized service
        "403":
          description: The request has been denied.
        "200":
          description: Profile
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResponseProfile'
      security:
      - bearerAuth: []
components:
  schemas:
    RequestProfile:
      properties:
        externalCustomerId:
          maxLength: 12
          type: integer
          description: External Customer Id
          example: 220
    ResponseProfile:
      properties:
        success:
          maxLength: 1
          type: boolean
          description: Result process
          example: true
        email:
          maxLength: 40
          type: string
          description: Email
          example: j1106carloslibra@hotmail.com
        phoneNumber:
          maxLength: 10
          type: string
          description: Phone number
          example: "525568576731"
        nivel:
          maxLength: 10
          type: string
          description: Level account
          example: "1"
        message:
          $ref: '#/components/schemas/Message'
    Message:
      type: object
      properties:
        code:
          maxLength: 10
          type: integer
          description: Result Code
          example: 0
        message:
          maxLength: 500
          type: string
          description: Result Mesagge Code
          example: success
  responses:
    Unauthorized:
      description: Unauthorized service
    NotFound:
      description: The information does not exist
    BadRequest:
      description: The data sent is incorrect or corrupted
    ServerError:
      description: An error occurred on the server
    "403Forbidden":
      description: The request has been denied.
  parameters:
    token:
      name: token
      in: header
      description: Authentication token
      required: true
      style: simple
      explode: false
      schema:
        type: string
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
