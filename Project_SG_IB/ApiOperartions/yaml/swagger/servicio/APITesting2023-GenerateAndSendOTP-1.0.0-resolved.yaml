---
openapi: 3.0.0
info:
  title: Generate and Send OTPs
  description: Service to generate and send an OTP to the client.
  version: 1.0.0
servers:
- url: https://virtserver.swaggerhub.com/APITesting2023/GenerateAndSendOTP/1.0.0
  description: SwaggerHub API Auto Mocking
tags:
- name: Generate and Send OTPs
  description: Service to generate and send an OTP to the client.
paths:
  /apiOperations/password/generateTransactionFactor:
    post:
      tags:
      - Generate and Send OTPs
      summary: Service to generate and send an OTP to the client
      operationId: generateTransactionFactor
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RequestOtp'
        required: true
      responses:
        "400":
          description: The data sent is incorrect or corrupted
        "500":
          description: An error occurred on the server
        "401":
          description: Unauthorized service
        "403":
          description: The request has been denied.
        "200":
          description: Get OTP
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResponseOtp'
      security:
      - bearerAuth: []
components:
  schemas:
    RequestOtp:
      properties:
        externalCustomerId:
          maxLength: 12
          type: integer
          description: External Customer Id
          example: 220
    ResponseOtp:
      properties:
        success:
          type: boolean
          description: result process
          example: true
        message:
          $ref: '#/components/schemas/Message'
    Message:
      type: object
      properties:
        code:
          maxLength: 10
          type: integer
          description: Result Code
          example: 0
        message:
          maxLength: 500
          type: string
          description: Result Mesagge Code
          example: success
  responses:
    Unauthorized:
      description: Unauthorized service
    NotFound:
      description: The information does not exist
    BadRequest:
      description: The data sent is incorrect or corrupted
    ServerError:
      description: An error occurred on the server
    "403Forbidden":
      description: The request has been denied.
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
