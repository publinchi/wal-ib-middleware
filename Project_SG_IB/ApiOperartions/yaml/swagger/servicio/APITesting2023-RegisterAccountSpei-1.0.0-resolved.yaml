---
openapi: 3.0.0
info:
  title: Register Account Spei
  description: Service to register beneficiaries for spei transfers..
  version: 1.0.0
servers:
- url: https://virtserver.swaggerhub.com/APITesting2023/RegisterAccountSpei/1.0.0
  description: SwaggerHub API Auto Mocking
tags:
- name: Register Account Spei
  description: Service to register beneficiaries for spei transfers.
paths:
  /apiOperations/accounts/registerAccount:
    post:
      tags:
      - Register Account Spei
      summary: Service to register beneficiaries for spei transfers.
      operationId: registerAccount
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RequestRegisterAccountSpei'
        required: true
      responses:
        "400":
          description: The data sent is incorrect or corrupted
        "500":
          description: An error occurred on the server
        "401":
          description: Unauthorized service
        "403":
          description: The request has been denied.
        "200":
          description: Register account
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResponseRegisterAccountSpei'
      security:
      - bearerAuth: []
components:
  schemas:
    RequestRegisterAccountSpei:
      properties:
        accountNumber:
          maxLength: 20
          type: string
          description: account number
          example: "04000000027"
        accountNumberDestination:
          maxLength: 20
          type: string
          description: account number
          example: "5568576731"
        typeDestinationId:
          maxLength: 2
          type: integer
          description: Type destination Id, please check catalog "bv_tipo_cta_trn_ter
          example: 0
        externalCustomerId:
          maxLength: 12
          type: integer
          description: External Customer Id
          example: 220
    ResponseRegisterAccountSpei:
      properties:
        success:
          type: boolean
          description: result process
          example: true
        statusRegister:
          maxLength: 20
          type: string
          description: Registration result
          example: existing
        message:
          $ref: '#/components/schemas/Message'
    Message:
      type: object
      properties:
        code:
          maxLength: 10
          type: integer
          description: Result Code
          example: 0
        message:
          maxLength: 500
          type: string
          description: Result Mesagge Code
          example: success
  responses:
    Unauthorized:
      description: Unauthorized service
    NotFound:
      description: The information does not exist
    BadRequest:
      description: The data sent is incorrect or corrupted
    ServerError:
      description: An error occurred on the server
    "403Forbidden":
      description: The request has been denied.
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
