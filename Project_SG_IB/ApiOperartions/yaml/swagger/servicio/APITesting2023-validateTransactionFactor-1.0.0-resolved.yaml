---
openapi: 3.0.0
info:
  title: ValidateTransactionFactor
  description: Validate Transactio Factor API
  version: 1.0.0
servers:
- url: https://virtserver.swaggerhub.com/APITesting2023/validateTransactionFactor/1.0.0
  description: SwaggerHub API Auto Mocking
tags:
- name: ValidateTransactionFactor
  description: Validate Transactio Factor API
paths:
  /apiOperations/password/validateTransactionFactor:
    post:
      tags:
      - ValidateTransactionFactor
      summary: Valdate token transaction factor API
      operationId: validateTransactionFactor
      requestBody:
        description: Request ValidateTransactionFactor
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ValidateTokenRequest'
        required: true
      responses:
        "400":
          description: The data sent is incorrect or corrupted
        "500":
          description: An error occurred on the server
        "401":
          description: Unauthorized service
        "403":
          description: The request has been denied.
        "200":
          description: Response Create Customer
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ValidateTokenResponse'
      security:
      - bearerAuth: []
components:
  schemas:
    ValidateTokenRequest:
      properties:
        externalCustomerId:
          maxLength: 12
          type: integer
          description: External Customer Id
          example: 220
        token:
          maxLength: 7
          minLength: 4
          type: string
          description: otp number
          example: "47849"
    ValidateTokenResponse:
      properties:
        success:
          type: boolean
          description: result process
          example: true
        message:
          $ref: '#/components/schemas/Message'
    Message:
      type: object
      properties:
        code:
          maxLength: 10
          type: integer
          description: Result Code
          example: 0
        message:
          maxLength: 500
          type: string
          description: Result Mesagge Code
          example: success
  responses:
    Unauthorized:
      description: Unauthorized service
    NotFound:
      description: The information does not exist
    BadRequest:
      description: The data sent is incorrect or corrupted
    ServerError:
      description: An error occurred on the server
    "403Forbidden":
      description: The request has been denied.
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
